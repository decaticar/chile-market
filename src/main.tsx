import React from "react";
import ReactDOM from "react-dom/client";
import ChileMarketTracker from "./chile-market-tracker";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ChileMarketTracker />
  </React.StrictMode>
);
